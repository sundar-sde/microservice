server:
  port: 8083

spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver: org.h2.driver
    username: ms
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
